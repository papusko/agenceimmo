{% extends 'base.html.twig' %}

{% block title %}<h1>Edition immobilière!</h1>{% endblock %}

{% block body %}

#{% form_theme form 'bootstrap_4_layout.html.twig' %}
<div class="container mt-4">
    <h2 class="alert">Modification des biens  ✅</h2>
    <div class="panel panel-default">
  <div class="panel-body"> <section class="text-right">
  <a href="/agent/creer"><button class="btn btn-secondary">Ajouter un nouveau agent</button></a>
  </br></br>
  <a href="/agent/creer"><button class="btn btn-primary">Ajouter un nouveau agent</button></a></br></br>
  <a href="/agent/creer"><button class="btn btn-secondary">Ajouter un nouveau agent</button></a>
  <p>Vous pouvez directement creer un autre agent ici</p>
</section></div>
</div>
    {{ form_start(form) }}
       

    			    <div class="row">
		    	<div class="col-md-4">{{ form_row(form.prenomAgent) }}</div>
		    	<div class="col-md-4">{{ form_row(form.nomAgent) }}</div>	
		    </div>
		    <div class="row">
		    	<div class="col-md-4">{{ form_row(form.adresseAgent) }}</div>
		    	<div class="col-md-4">{{ form_row(form.telephone) }}</div>	
		    </div>
    {{ form_widget(form) }}
    <button class=)"btn btn-prymary"> valider</button>
    {{ form_end(form) }}
   
</div>
{% endblock %}
