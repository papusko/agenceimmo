{% extends 'base.html.twig' %}

{% block title %}Autres{% endblock %}

{% block body %}
    <h1>Autres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ autre.id }}</td>
            </tr>
            <tr>
                <th>Essaie</th>
                <td>{{ autre.essaie }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ autre.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('autres_index') }}">back to list</a>

    <a href="{{ path('autres_edit', {'id': autre.id}) }}">edit</a>

    {{ include('autres/_delete_form.html.twig') }}
{% endblock %}
